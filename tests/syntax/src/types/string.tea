#main

num = 123
str = "hello "
key = "f3"

echo "some string".length

echo str.trim() // hello

str#Bytes.copy(2) // "llo "
str.copy(0, 3) // "hel"

arr = [
	'f1': 123,
	"f2": "hi",
	"f3": "字符串",
	"sub": [1, 2, 3]
]

echo "${str}world" // hello world
echo '$str\n
	- ${arr['f1']}
	- ${arr[key]}
'

// raw text
echo #text "$str world" // $str world
echo #text '
	- ${arr[\'f1\']}
	- ${arr[key]}
'

var abc = 'hhh'

'string${abc}';
"string${abc}"

html_escape("string${abc}\n")
html_escape('string')
html_escape('string<br>' .+ abc)

var v_10 String = "sth."
var v_11 = 123

var v_20 String
v_21 = "sth."
