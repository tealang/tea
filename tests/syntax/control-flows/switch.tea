#main

num = 1
str = 'abc'

#outer switch num {
	case num * 3:
		switch str {
		case 'some${num}', 'hello', ('a' concat 123):
			str = num * 5
			echo str
		case 'a':
			num = num * 2
			break #outer
		}
		else {
			echo 'not matched.'
		}

	case 10, 100:
		echo num
}
elseif num > 10 {
	echo 'num is greater than 10.'
}
catch ex {
	//
}
finally {
	echo 'finally!'
}

