---
The Coroutine syntax draft
---

#main

#php usleep(micro_seconds UInt)

internal co_test() {
	// 本函数中有使用协程
	// 编译器将自动添加 协程容器创建语句 到函数体的开头
	// 使用协程的Unit，将在入口文件末尾添加 wait 语句
	for _ = 1 to 100 {
		// 使用 #co 标记的语句，编译器将自动创建协程
		#co for _ = 1 to 100 {
			usleep(1000)
		}
	}

	echo 'the Coroutine executing...'
}

co_test()

// end
