
list = ['a', 'b']

// push, append at the end
list[] = 'c'

var_dump(list)

// pop, take at the end
last_element = list[]

var_dump(last_element)

// // unshift, insert at the begin
// []list = 'd'

// // shift, take at the begin
// first_element = []list

